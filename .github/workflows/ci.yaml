name: challenge
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '15.x'
      - run: npm i
      - run: npm run lint
      - run: npm run prettier
      - run: npm run test
      - run: npm run build